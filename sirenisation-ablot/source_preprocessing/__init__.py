"""Named, reproducible preparation code for the three independent source families.

Import operational entry points explicitly from :mod:`source_preprocessing.jobs`.
Keeping the package initializer side-effect free also makes
``python -m source_preprocessing.jobs`` a clean command-line entry point.
"""
